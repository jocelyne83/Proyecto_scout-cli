# Ignorar todo por defecto
*

# Incluir solo lo necesario !install.sh


# Incluir solo el archivo comprimido de dist
!dist/
dist/*
!dist/*.tar.gz

# Incluir solo YAML de docs
!docs/
docs/*
!docs/*.yaml
